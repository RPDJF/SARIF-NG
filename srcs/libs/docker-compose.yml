services:
  libs:
    build: ./
    container_name: sarif-ng-libs
    environment:
      - TZ=Europe/Zurich
    networks:
      - sarifng-net
    volumes:
      - sarif-libs:/libs/

volumes:
  sarif-libs:
    name: sarif-libs
    driver: local
