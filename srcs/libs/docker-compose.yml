services:
  sarif-ng-libs:
    build: ./
    container_name: sarif-ng-libs
    environment:
      - TZ=Europe/Zurich
    networks:
      - sarifng-net
    volumes:
      - sarifng-libs:/libs/

volumes:
  sarifng-libs:
    name: sarif-libs
    driver: local
