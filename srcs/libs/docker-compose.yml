services:
  sarif-ng-libs:
    build: ./
    container_name: sarif-ng-libs
    environment:
      - TZ=Europe/Zurich
    networks:
      - bw-services
    volumes:
      - sarifng-libs:/libs/

volumes:
  sarifng-libs:
    name: sarif-libs
    driver: local
